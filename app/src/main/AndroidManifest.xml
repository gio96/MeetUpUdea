<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.galonsogomez.meetupudea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ShowGroupActivity"
            android:parentActivityName=".BottomNavActivity" />
        <activity
            android:name=".BottomNavActivity"
            android:label="@string/app_name" />
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".CreateGroupElementsActivity" />
        <activity android:name=".ShowEventActivity"></activity>
    </application>

</manifest>